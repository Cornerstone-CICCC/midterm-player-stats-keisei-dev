# PostgreSQL connection (used by the Express backend)
PGHOST=localhost
PGPORT=5432
PGUSER=postgres
PGPASSWORD=
PGDATABASE=worldcup

# Express server port
PORT=3000

# Public base URL of the API, consumed by the Astro front end
PUBLIC_API_BASE_URL=http://localhost:3000
